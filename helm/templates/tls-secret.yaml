{{- if .Values.ingress.tls }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.ingress.tlsSecret }}
  namespace: {{ .Release.Namespace }}
type: kubernetes.io/tls
data:
  tls.crt: {{ .Values.ingress.tlsCrt | quote }}
  tls.key: {{ .Values.ingress.tlsKey | quote }}
{{- end }}
